import * as Styled from '../styles/skeleton';

function Skeleton() {
  return (
    <Styled.Card>
      <Styled.Thumnail />
      <Styled.Title />
      <Styled.SubTitle />
    </Styled.Card>
  );
}

export default Skeleton;
