import images from './images';
import icons from './icons';
import { COLORS } from './theme';

const STYLE = {
  HEADER_WIDTH: '250px',
  HEADER_HEIGHT: '80px',
  PLAYER_HEIGHT: '80px',
};

export { images, icons, COLORS, STYLE };
